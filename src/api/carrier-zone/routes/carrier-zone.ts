/**
 * carrier-zone router
 */

import { factories } from "@strapi/strapi";
import { CARRIER_ZONE_ROUTE } from "../../../helper/constants";

export default factories.createCoreRouter(CARRIER_ZONE_ROUTE);
