/**
 * carrier-zone controller
 */

import { factories } from "@strapi/strapi";
import { CARRIER_ZONE_ROUTE } from "../../../helper/constants";

export default factories.createCoreController(CARRIER_ZONE_ROUTE);
